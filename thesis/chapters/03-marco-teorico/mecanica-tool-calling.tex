\subsection{Mecánica de Tool Calling}
% TODO: Flujo completo de una llamada a herramienta:
% Prompt → LLM → tool_use → servidor → resultado → LLM → respuesta.
% Overhead de tokens: serialización JSON, schemas, tool descriptions.
% Comparar con ejecución de código: Prompt → LLM → código → sandbox → resultado.
